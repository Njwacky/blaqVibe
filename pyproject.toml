[tool.ruff]
line-length = 120
target-version = "py312"
exclude = ["migrations", ".venv", "docs"]

[tool.ruff.lint]
# Start with the "definitely a bug" rules; widen over time.
select = ["E9", "F63", "F7", "F82"]
